services:
  user-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: universearch-user-service
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3001
    ports:
      - "3001:3001"
    restart: unless-stopped
